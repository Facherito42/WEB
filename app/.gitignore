node_modules
dist
.DS_Store
*.local
run-dev.cmd

# Duplicated from the repo-root img/ (already tracked there) so Vite can
# serve it from public/ — not tracked here to avoid a second copy in git.
public/img
